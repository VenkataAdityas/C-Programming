#include<stdio.h>

int main() {
    int n=10;
    //for(initialize;test(condition);increment/decrement(updating loop counter))
    for(int i=0;i<n;i++){
        printf("The value of i is %d\n",i);
    }
    return 0;
}